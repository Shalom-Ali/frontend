<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Welcome to Your Learning Dashboard</h1>
  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>
  <h2>Content for You</h2>
  <p>Module: <%= content.module %></p>
  <p>Details: <%= content.details %></p>
  <form action="/progress" method="POST">
    <input type="hidden" name="user_id" value="<%= user_id %>">
    <label for="module">Module:</label>
    <input type="text" id="module" name="module" value="<%= content.module %>" readonly>
    <label for="progress">Progress (%):</label>
    <input type="number" id="progress" name="progress" min="0" max="100" required>
    <button type="submit">Update Progress</button>
  </form>
</body>
</html>
